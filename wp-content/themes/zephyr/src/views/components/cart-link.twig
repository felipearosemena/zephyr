{% if not is_checkout %}
<button
  v-on:click.prevent="toggleCart"
  class="{{ namespace }}__cart cart-link">
  cart
  <span v-html="cart.count"></span>
</button>
{% else %}
<span class="{{ namespace }}__cart cart-link">
  cart
  <span v-html="cart.count"></span>
</span>
{% endif %}
