{% set image = TimberImage(term.meta('thumbnail_id')) %}

<shape-item
  href="{{ term.path }}"
   :term='{{ term|json_encode }}'
   label="{{ label }}">

  <template slot="svg">
    {% include 'components/shapes/' ~ term.name ~ '.svg' %}
  </template>

  <template slot="image">
    {{ macros.img( term.meta('thumbnail_id') ) }}
  </template>

</shape-item>
